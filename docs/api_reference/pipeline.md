# Pipeline API

Auto-generated from docstrings. See [Architecture](../developer_guide/extensibility.md) for how
these pieces fit together, and `CLAUDE.md` in the repository root for the canonical-vs-legacy
config split. `SimulationEngine` is the canonical path for all new development; the legacy
`GeneralizedTactileEncodingPipeline` is documented inline in
`sensoryforge/core/generalized_pipeline.py`.

## SimulationEngine

::: sensoryforge.core.simulation_engine.SimulationEngine
    options:
      members:
        - run
