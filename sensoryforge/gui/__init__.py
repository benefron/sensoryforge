"""SensoryForge GUI — interactive workbench for sensory encoding experiments.

The GUI is the primary experimentation tool: design receptor grids, create
stimuli, configure neuron models, and observe population responses in real time.
Once a configuration is validated, export to YAML for batch execution via CLI.
"""
