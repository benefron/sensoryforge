"""Standalone testing helpers for SensoryForge and its plugins.

This package has no Qt or GUI dependency and only depends on ``torch`` plus
the rest of ``sensoryforge`` -- it is safe to import from a third-party
plugin package's own test suite (see :mod:`sensoryforge.testing.contracts`).
"""
