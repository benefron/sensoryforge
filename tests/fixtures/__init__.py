"""Test fixtures for SensoryForge unit and integration tests.

This module provides shared fixtures, configuration files, and test data
used across the test suite.

Fixtures:
    - test_config.yml: Standard test configuration for pipeline components
    - small_config.yml: Minimal configuration for fast tests
    - (future) Pre-generated test stimuli and expected outputs
"""
